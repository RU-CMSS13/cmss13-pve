// /obj/effect/abstract/particle_holder/var/particle_flags
// Flags that affect how a particle holder displays something.

/// If the parent is inside a mob, display off that mob too.
#define PARTICLE_ATTACH_MOB (1<<0)
